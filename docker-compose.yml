services:
  kda-query-hub:
    image: kda-query-hub:latest
    build: .
    container_name: kda-query-hub
    restart: unless-stopped
    ports:
      - "${HOST_PORT:-8092}:8092"
    env_file:
      - .env
    environment:
      TZ: ${TZ:-Asia/Shanghai}
    volumes:
      - ./data:/app/data

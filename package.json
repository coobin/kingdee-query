{
  "name": "kingdee-query-hub",
  "version": "0.1.0",
  "private": true,
  "description": "SSO protected Kingdee K3Cloud query portal and Dify tool API",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check src/access-control.js && node --check public/app.js && node --check public/login.js && node --check public/admin.js && node --test test/*.test.js",
    "test": "node --test test/*.test.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@simplewebauthn/server": "^13.3.2",
    "express": "^4.21.2"
  }
}
